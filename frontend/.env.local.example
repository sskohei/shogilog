# サーバー専用（NEXT_PUBLIC_ を付けない）。バックエンド API のベース URL。
BACKEND_API_BASE_URL=http://localhost:8000/api/v1

# AUTH-3 (#12) で本物の Supabase ログインに置き換えるまでの暫定措置。
# backend と同じ SUPABASE_JWT_SECRET で手動発行した、Supabase Auth テストユーザー向けの JWT を設定する。
DEV_AUTH_TOKEN=
