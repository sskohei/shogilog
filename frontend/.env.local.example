# サーバー専用（NEXT_PUBLIC_ を付けない）。バックエンド API のベース URL。
BACKEND_API_BASE_URL=http://localhost:8000/api/v1

# ブラウザにも公開される（NEXT_PUBLIC_ プレフィックス必須）。Supabase プロジェクトの URL / anon key。
# backend/.env の SUPABASE_URL / SUPABASE_ANON_KEY と同じプロジェクトを指定する。
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
